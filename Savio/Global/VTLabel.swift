//
//  VTLabel.swift
//  BlazeTrail
//
//  Created by Mangesh on 31/08/15.
//  Copyright (c) 2015 Mangesh  Tekale. All rights reserved.
//

import UIKit

let lblStyleA1 = "A1" // Typo For Splash Screen dark blue
let lblStyleA2 = "A2" // Normal Heading white
let lblStyleA3 = "A3" // Normal Subheading white
let lblStyleA4 = "A4" // Typo For Stats View in Dashboard Screen white
let lblStyleA5 = "A5" // Typo For Industries  View in Dashboard Screen white for count
let lblStyleA2BD = "A2BD" // Normal Heading blue dark
let lblStyleA3BD = "A3BD" // Normal Subheading white
let lblStyleA4BD = "A4BD" // Typo For Stats View in Dashboard Screen dark blue
let lblStyleA5BD = "A5BD" // Typo For Industries  View in Dashboard Screen dark blue for count

class VTLabel: UILabel {
    
    /*
    var style: String {
        get {
            return self.style
        }
        set(style) {
            switch  style {
            case lblStyleA1:
                self.font = FontCodes.fontForCode(code: fontHelveticaNeue_Bold, size: 28)
                self.textColor = ColorCodes.colorForCode(colorBlueBgDark)
                
            case lblStyleA2:
                self.font = FontCodes.fontForCode(code: fontHelveticaNeue_Bold, size: 12)
                self.textColor = UIColor.whiteColor()
                
            case lblStyleA2BD:
                self.font = FontCodes.fontForCode(code: fontHelveticaNeue_Bold, size: 12)
                self.textColor = ColorCodes.colorForCode(colorBlueBgDark)
                
            case lblStyleA3:
                self.font = FontCodes.fontForCode(code: fontHelveticaNeue, size: 11)
                self.textColor = UIColor.whiteColor()
                
            case lblStyleA3BD:
                self.font = FontCodes.fontForCode(code: fontHelveticaNeue, size: 11)
                self.textColor = ColorCodes.colorForCode(colorBlueBgDark)
                
            case lblStyleA4:
                self.font = FontCodes.fontForCode(code: fontHelveticaNeue_Bold, size: 28)
                self.textColor = UIColor.whiteColor()
                
            case lblStyleA4BD:
                self.font = FontCodes.fontForCode(code: fontHelveticaNeue_Bold, size: 28)
                self.textColor = ColorCodes.colorForCode(colorBlueBgDark)
                
            case lblStyleA5:
                self.font = FontCodes.fontForCode(code: fontHelveticaNeue_Bold, size: 24)
                self.textColor = UIColor.whiteColor()
                
            case lblStyleA5BD:
                self.font = FontCodes.fontForCode(code: fontHelveticaNeue_Bold, size: 24)
                self.textColor = ColorCodes.colorForCode(colorBlueBgDark)
            default: print("Default Case Reached")
            }
        }
    }    
 */
}
